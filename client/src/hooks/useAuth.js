import { useAuth } from '../contexts/AuthContext';

// This is just a re-export of the useAuth hook from AuthContext
// Keeping it here for consistency with the folder structure
export default useAuth;